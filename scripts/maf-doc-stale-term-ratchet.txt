# Known-pending stale-internal-name sites, as `<path> <count>`.
#
# `CachedDurableAgent` / `ComposeDurableAgent` / `ResolveDurableAgent` are identifiers that no
# longer exist. Every surviving mention is a comment or a sentence describing code that was renamed.
#
# Same ratchet rule as the factory-first list: a file may not EXCEED its number, an unlisted file may
# have none, and when a mention is removed the script prints a notice telling you to lower the count.
# Numbers here may only go DOWN. Line numbers are deliberately not used — they churn.

src/TemporalCommunity.Extensions.Agents/DurableAgentBuilder.cs 1
