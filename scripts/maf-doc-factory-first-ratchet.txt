# Known-pending factory-first `AddTool` defect sites, as `<doc-path> <count>`.
#
# THIS LIST IS EMPTY, AND THAT IS THE POINT.
#   `AddTool` has two overloads: AddTool(AIFunction tool, ...) and
#   AddTool(string name, Func<IServiceProvider, AIFunction> factory, ...). The factory overload
#   takes the NAME FIRST. Passing the factory lambda as the first argument —
#   `AddTool(sp => AIFunctionFactory.Create(...))` — binds to neither and does not compile.
#
#   All nine original sites (quickstart 1, usage 3, tool-interceptor 5) are fixed, so
#   verify-maf-doc-api-contracts.sh now fails on ANY occurrence in ANY MAF doc. Keep it that way.
#
# IF YOU ARE ABOUT TO ADD A LINE HERE
#   Don't. Fix the doc instead — the corrected form is two characters of thought and the compiled
#   harness in tests/docs/DocSnippets/ will confirm it. This file exists only so the mechanism
#   survives; numbers may only go DOWN, and there is nowhere below zero.
