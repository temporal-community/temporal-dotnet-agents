# Snippets that cannot be verbatim copies of their doc block, as `<key><TAB><reason>`.
#
# An entry EXEMPTS that snippet from line-by-line comparison, so each one is a hole in the gate.
# Two things keep the list honest: an entry whose key no longer exists fails, and an entry whose
# snippet has become an exact match fails. Add one only for a structural impossibility, never to
# silence drift — re-syncing the snippet is almost always the right fix.

docs/how-to/MAF/usage.md#inheritance--per-agent-vs-worker-level	Prose claim, not a fenced block: the doc writes `opts.RetryPolicy = ...` with `...` standing in for a policy value, which does not compile. The harness substitutes a real argument.
docs/how-to/MAF/testing-agents.md#custom-host-per-test	The doc's [Fact] closes over `fixture` and `myChatClient` from surrounding prose. A compile-only harness has no such scope, so they become method parameters — which is also why xUnit1001 is in this project's NoWarn.
