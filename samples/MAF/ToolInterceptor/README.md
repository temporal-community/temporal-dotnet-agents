# ToolInterceptor

Demonstrates `IAgentToolInterceptor` (Feature L) and workflow-parked HITL approval via `RequireApproval()` (Feature A) using a customer refund agent.

## Overview

The sample registers an `OrderInterceptor` that fires before every `apply_refund` tool call and exercises all four interceptor decision paths in three sequential turns:

| Turn | Order | Amount | Interceptor decision | Effect |
|---|---|---|---|---|
| 1 | ORD-999 (unknown) | — | `Skip` | Synthetic "not found" result; tool never dispatches |
| 2 | ORD-002 | $750.00 | `Block` | Policy violation; tool never dispatches |
| 3 | ORD-001 | $29.99 | `PauseForApproval` | Turn loop parks; program auto-approves |

`apply_refund` also has `RequireApproval()` set — this is the Rule 2 absolute floor. Even if the interceptor returned `Proceed`, the tool would still wait for human approval.

## What it demonstrates

- `IAgentToolInterceptor.BeforeToolCallAsync` — all four decision outcomes in one coherent flow
- `DurableToolDecision.Skip` — synthetic result injected; tool activity never dispatched
- `DurableToolDecision.Block` — error result injected; tool activity never dispatched
- `DurableToolDecision.PauseForApproval` — enriched description provided to the reviewer
- `DurableToolOptions.RequireApproval()` — configuration-time floor independent of interceptor
- `DurableToolOptions.SkipInterceptor()` — opt read-only `lookup_order` out of the interceptor
- `ITemporalAgentClient.GetPendingApprovalAsync` / `ResolveApprovalAsync` — approval poll/resolve loop

> **Library split:** `DurableToolDecision`, `DurableToolContext`, and `IDurableToolInterceptor<TContext>` are defined in `TemporalCommunity.Extensions.AI.Tools`. `IAgentToolInterceptor` and `AgentToolContext` are in `TemporalCommunity.Extensions.Agents.Tools`. Add `using TemporalCommunity.Extensions.AI.Tools;` to any file that references `DurableToolDecision` directly; add `using TemporalCommunity.Extensions.Agents.Tools;` for `IAgentToolInterceptor` / `AgentToolContext`.

> **`IDurableToolInterceptor<DurableToolContext>` sample** — see `samples/MEAI/ToolInterceptor` for the base-library interface in action. Interceptors run inside `DurableChatWorkflow.ExecuteDurableToolLoopTurnAsync` (the durable tool-dispatch loop).

## Prerequisites

- .NET 10 SDK
- Temporal Service 1.31.0 or newer on `localhost:7233` (local: `temporal server start-dev`)
- OpenAI API key configured via user-secrets:
  ```bash
  dotnet user-secrets set "OPENAI_API_KEY" "sk-..." --project samples/MAF/ToolInterceptor
  ```

## Configure credentials

```bash
dotnet user-secrets set "OPENAI_API_KEY" "sk-..." --project samples/MAF/ToolInterceptor
```

Optional — override the Temporal server address:
```bash
dotnet user-secrets set "TEMPORAL_ADDRESS" "your-server:7233" --project samples/MAF/ToolInterceptor
```

## Run

```bash
dotnet run --project samples/MAF/ToolInterceptor/ToolInterceptor.csproj
```

## Expected output

```
Worker started. Running three-turn refund scenario...

=== Turn 1: Nonexistent order ===
User : I need a refund for order ORD-999
Agent: I tried to look up order ORD-999, but it was not found. No refund was processed.

=== Turn 2: Large refund (blocked) ===
User : What about order ORD-002? I want a full refund.
Agent: I was unable to process the refund for order ORD-002. The refund amount of $750.00
       exceeds the $500 automatic approval limit. Please contact a supervisor to proceed.

=== Turn 3: Valid refund (approval required) ===
User : Please refund $29.99 for order ORD-001
[Approval requested] Apply refund of $29.99 for Jane Doe — MacBook Pro, Order ORD-001
[Auto-approving for demo]
Agent: The refund of $29.99 for order ORD-001 has been successfully applied.

Done.
```

The exact agent wording varies because it is generated by the LLM.

## See also

- [`docs/how-to/MAF/tool-interceptor.md`](../../../docs/how-to/MAF/tool-interceptor.md) — full `IAgentToolInterceptor` reference
- [`docs/how-to/MAF/hitl-patterns.md`](../../../docs/how-to/MAF/hitl-patterns.md) — approval dashboard API, `ResolveApprovalAsync`
- [`samples/MAF/HumanInTheLoop/`](../HumanInTheLoop/) — interactive in-tool HITL via `RequestApprovalAsync`
